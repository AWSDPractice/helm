kind: Service
apiVersion: v1

metadata:
  name: nginx-lb

spec:
  type: LoadBalancer
  selector: # here ABLES ARE USED AS SELECETORS
    app: nginx
    tier: frontend
    purpose: deployment
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: {{ .Values.service.nodePort }}